---
description: Git hygiene and commit constraints.
globs: *
---
# Git Rules

- **Repository Rule (Git/Commits):** Never commit generated pipeline artifacts to version control. This includes but is not limited to:
  - `.wav` audio files
  - `.json` timestamp payloads generated by the pipeline
  - `.mp4` video files
  - Any contents within the `data/workspace/` directory
